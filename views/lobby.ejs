<!DOCTYPE html>
<html>
<head>
    <title>活動大廳</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0">
    <meta name="theme-color" content="#388697">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-color: #388697;
            --bg-color: #F2F4F6;
            --card-bg: #FFFFFF;
            --text-main: #1C1C1E;
            --text-sub: #8E8E93;
            --radius-lg: 20px;
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.04);
            --header-height: 60px;
        }
        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            padding-top: calc(var(--header-height) + 20px); 
            padding-bottom: 100px;
            min-height: 100vh;
            margin: 0;
        }

        /* Header */
        .top-nav {
            position: fixed; top: 0; left: 0; right: 0;
            height: var(--header-height);
            background: var(--primary-color);
            color: white;
            display: flex; align-items: center; justify-content: center; /* Center title */
            padding: 0 20px;
            z-index: 1000;
            box-shadow: 0 4px 12px rgba(56, 134, 151, 0.2);
            padding-top: env(safe-area-inset-top); 
            height: calc(var(--header-height) + env(safe-area-inset-top));
        }
        .top-nav-title { font-weight: 700; font-size: 18px; }

        /* Modern Activity Card */
        .activity-card {
            background: var(--card-bg);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            padding: 20px;
            margin-bottom: 16px;
            border: none;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            display: block;
            text-decoration: none;
            color: var(--text-main);
            position: relative;
            overflow: hidden;
        }
        .activity-card:active { transform: scale(0.98); background-color: #F8FDFF; }

        .act-icon {
            width: 40px; height: 40px;
            background: rgba(56, 134, 151, 0.1);
            color: var(--primary-color);
            border-radius: 12px;
            display: flex; align-items: center; justify-content: center;
            font-size: 1.2rem;
            margin-right: 15px;
        }
        .act-arrow { color: #E5E5EA; font-size: 1.2rem; }

        /* Action Button */
        .action-btn-container { position: fixed; bottom: 30px; left: 20px; right: 20px; z-index: 900; }
        .action-btn {
            width: 100%; border-radius: 16px; font-size: 1.1rem; font-weight: 700; padding: 14px;
            box-shadow: 0 8px 20px rgba(56, 134, 151, 0.4); border: none; background: var(--primary-color); color: white;
            display: flex; align-items: center; justify-content: center;
        }
        
        /* Modal styling tweaks */
        .modal-content { border-radius: var(--radius-lg); border: none; }
        .modal-header { border-bottom: 1px solid #F2F4F6; }
        .modal-footer { border-top: 1px solid #F2F4F6; }
        .form-control { background-color: #F2F4F6; border: none; padding: 12px; border-radius: 12px; }
        .form-select { background-color: #F2F4F6; border: none; padding: 12px; border-radius: 12px; }

        ::-webkit-scrollbar { display: none; }
    </style>
</head>
<body>

<div class="top-nav">
    <div class="top-nav-title">活動大廳</div>
</div>

<div class="container">
    <% if (activities.length === 0) { %>
        <div class="text-center py-5 text-muted" style="margin-top: 100px;">
            <i class="fa-solid fa-clipboard-list fa-3x mb-3" style="color: #E5E5EA;"></i>
            <p class="fw-bold">暫時沒有活動</p>
            <p class="small">點擊下方按鈕建立第一個活動</p>
        </div>
    <% } else { %>
        <div class="mb-3 small text-muted fw-bold">最近活動</div>
        <% activities.forEach(act => { %>
            <a href="/activity/<%= act.id %>" class="activity-card">
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                        <div class="act-icon">
                            <% if (act.type === 'bowling') { %>
                                <i class="fa-solid fa-bowling-ball"></i>
                            <% } else { %>
                                <i class="fa-solid fa-table-tennis-paddle-ball"></i>
                            <% } %>
                        </div>
                        <div>
                            <h5 class="fw-bold mb-1" style="color: var(--primary-color);"><%= act.name %></h5>
                            <div class="text-muted small">
                                <span class="me-2"><%= act.type === 'bowling' ? '保齡球' : 'Pickleball (夾錢)' %></span>
                                ‧ <span class="ms-2"><%= new Date(act.created_at).toLocaleDateString() %></span>
                            </div>
                        </div>
                    </div>
                    <i class="fa-solid fa-chevron-right act-arrow"></i>
                </div>
            </a>
        <% }) %>
    <% } %>
</div>

<div class="action-btn-container">
    <button class="action-btn" data-bs-toggle="modal" data-bs-target="#createModal">
        <i class="fa-solid fa-plus me-2"></i> 建立新活動
    </button>
</div>

<div class="modal fade" id="createModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title fw-bold" style="color: var(--primary-color);">建立新活動</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form action="/create-activity" method="POST">
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">活動名稱</label>
                        <input type="text" name="name" class="form-control" placeholder="例如: 週末打波" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label fw-bold small text-muted">類型</label>
                        <select name="type" class="form-select">
                            <option value="bowling">🎳 保齡球 (按局數)</option>
                            <option value="pickleball">🏓 Pickleball (夾場租)</option>
                        </select>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-light rounded-pill px-4" data-bs-dismiss="modal" style="color: var(--text-sub);">取消</button>
                    <button type="submit" class="btn btn-primary rounded-pill px-4" style="background: var(--primary-color); border: none;">建立</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>